services:
  web:
    build:
      context: .
      args:
        APP_VERSION: ${APP_VERSION:-0.1.0}
    image: aidc-work:latest
    environment:
      TZ: Asia/Taipei
    ports:
      - "3163:80"
    restart: unless-stopped
